import type { InventoryStatus, Warehouse } from "@/app/lib/inventory-contract";
import { InventoryIcon, statusLabels } from "./inventory-ui";

export function InventoryFiltersBar({ query, warehouseId, status, warehouses, onQuery, onWarehouse, onStatus, onClear, children }: { query: string; warehouseId: string; status: "all" | InventoryStatus; warehouses: Warehouse[]; onQuery: (value: string) => void; onWarehouse: (value: string) => void; onStatus: (value: "all" | InventoryStatus) => void; onClear: () => void; children: React.ReactNode }) {
  return <div className="inventory-filter-bar"><div className="inventory-filter-controls"><label className="search"><InventoryIcon name="search" /><span className="sr-only">Buscar producto</span><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Buscar nombre o SKU..." /></label><select aria-label="Filtrar por depósito" value={warehouseId} onChange={(event) => onWarehouse(event.target.value)}><option value="">Todos los depósitos</option>{warehouses.map((warehouse) => <option key={warehouse.id} value={warehouse.id}>{warehouse.name}</option>)}</select><select aria-label="Filtrar por estado" value={status} onChange={(event) => onStatus(event.target.value as "all" | InventoryStatus)}><option value="all">Todos los estados</option>{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select><button className="link-button" onClick={onClear}>Limpiar filtros</button></div>{children}</div>;
}

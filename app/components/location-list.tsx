"use client";

import Link from "next/link";
import { FormEvent, useCallback, useEffect, useRef, useState } from "react";
import { ListSkeleton } from "@/app/components/list-skeleton";
import { emptyLocationForm, LocationDialog, LocationForm, LocationIcon, RolePicker, roles } from "@/app/components/location-ui";
import { locationApi } from "@/app/lib/location-api";
import type { LocationWarehouse, WarehouseLocationWithProducts } from "@/app/lib/location-contract";
import type { UserRole } from "@/app/lib/product-contract";
import { getLocationPermissions, validateLocationInput } from "@/app/lib/location-rules";

type Notice = { kind: "success" | "error"; text: string } | null;
type Dialog = "new" | "edit" | null;

export function LocationList({ warehouseId }: { warehouseId: string }) {
  const [warehouse, setWarehouse] = useState<LocationWarehouse | null>(null), [locations, setLocations] = useState<WarehouseLocationWithProducts[]>([]), [role, setRole] = useState<UserRole>("admin"), [loading, setLoading] = useState(true), [loadError, setLoadError] = useState(""), [notice, setNotice] = useState<Notice>(null), [dialog, setDialog] = useState<Dialog>(null), [editing, setEditing] = useState<WarehouseLocationWithProducts | null>(null), [form, setForm] = useState<LocationForm>(emptyLocationForm), [errors, setErrors] = useState<Partial<Record<keyof LocationForm, string>>>({}), [pending, setPending] = useState(false);
  const errorRef = useRef<HTMLDivElement>(null); const permissions = getLocationPermissions(role);
  const load = useCallback(async () => { setLoading(true); setLoadError(""); try { if (!navigator.onLine) throw new Error("Sin conexión a internet. Verificá tu conexión y reintentá."); const [warehouses, nextLocations] = await Promise.all([locationApi.listWarehouses(), locationApi.listLocations(warehouseId)]); setWarehouse(warehouses.find((item) => item.id === warehouseId) ?? null); setLocations(nextLocations); } catch (error) { setLoadError(error instanceof Error ? error.message : "No se pudieron cargar las ubicaciones."); } finally { setLoading(false); } }, [warehouseId]);
  useEffect(() => {
    // eslint-disable-next-line react-hooks/set-state-in-effect
    void load();
  }, [load]);
  useEffect(() => { if (Object.keys(errors).length) errorRef.current?.focus(); }, [errors]);
  const openDialog = (mode: Exclude<Dialog, null>, location?: WarehouseLocationWithProducts) => { setErrors({}); setEditing(location ?? null); setForm(location ? { code: location.code, aisle: location.aisle, rack: location.rack, level: location.level, sortOrder: location.sortOrder, isActive: location.isActive } : emptyLocationForm); setDialog(mode); };
  const save = async (event: FormEvent) => { event.preventDefault(); const nextErrors = validateLocationInput(form); setErrors(nextErrors); if (Object.keys(nextErrors).length) return; setPending(true); setNotice(null); try { await (dialog === "edit" && editing ? locationApi.updateLocation(editing.id, form) : locationApi.createLocation(warehouseId, form)); setDialog(null); await load(); setNotice({ kind: "success", text: dialog === "edit" ? "Ubicación actualizada." : "Ubicación creada." }); } catch (error) { setNotice({ kind: "error", text: error instanceof Error ? error.message : "No se pudo guardar la ubicación." }); } finally { setPending(false); } };
  const warehouseName = warehouse?.name ?? "este depósito";
  return <section className="workspace" id="ubicaciones"><header className="topbar"><div><Link href="/ubicaciones" className="back-link">← Depósitos</Link><p className="eyebrow">OPERACIONES / UBICACIONES</p><h1>Ubicaciones de {warehouseName}</h1><p className="subtitle">Gestioná racks y el orden de recorrido de picking para este depósito.</p></div><div className="top-actions"><RolePicker role={role} onChange={setRole} />{permissions.manage && <button className="button primary" onClick={() => openDialog("new")} disabled={loading}><LocationIcon name="plus" /> Nueva ubicación</button>}</div></header>{notice && <div className={`notice ${notice.kind}`} role="status"><span>{notice.kind === "success" ? "Listo" : "No se pudo completar"}</span>{notice.text}<button aria-label="Cerrar mensaje" onClick={() => setNotice(null)}><LocationIcon name="close" /></button></div>}<div className="permission-note">Estás operando como <strong>{roles[role]}</strong>. {permissions.manage ? "Podés crear y editar ubicaciones; la asignación de productos se hace desde el detalle de cada una." : "Sólo podés consultar las ubicaciones y su contenido."}</div><section className="list-panel location-page-list">{loading ? <ListSkeleton label="Cargando ubicaciones…" /> : loadError ? <div className="state error-state"><strong>No pudimos cargar las ubicaciones</strong><span>{loadError}</span><button className="button secondary" onClick={() => void load()}>Reintentar</button></div> : locations.length === 0 ? <div className="state"><LocationIcon name="pin" /><strong>Este depósito no tiene ubicaciones</strong><span>{permissions.manage ? "Creá la primera ubicación para comenzar a asignar productos." : "No hay ubicaciones disponibles para consultar."}</span></div> : <><div className="list-meta"><strong>{locations.length} {locations.length === 1 ? "ubicación" : "ubicaciones"}</strong><span>Ordenadas por recorrido de picking</span></div><ul className="product-list">{locations.map((location) => <li key={location.id}><div className="location-row-wrap"><Link className="product-row location-row" href={`/ubicaciones/${encodeURIComponent(warehouseId)}/${encodeURIComponent(location.id)}`}><span className="thumbnail"><LocationIcon name="pin" /></span><span className="product-main"><strong>{location.code}</strong><span>Pasillo {location.aisle} · Rack {location.rack} · Nivel {location.level}</span><span className="location-contents">{location.products.length ? location.products.map((product) => product.name).join(", ") : "Vacía"}</span></span><span className="location-order">#{location.sortOrder}</span><span className={`status ${location.isActive ? "active" : "inactive"}`}>{location.isActive ? "Activa" : "Inactiva"}</span></Link>{permissions.manage && <button className="row-edit" aria-label={`Editar ubicación ${location.code}`} onClick={() => openDialog("edit", location)}><LocationIcon name="edit" /></button>}</div></li>)}</ul></>}</section>{dialog && <LocationDialog mode={dialog} form={form} errors={errors} pending={pending} errorRef={errorRef} onClose={() => setDialog(null)} onChange={(key, value) => { setForm((current) => ({ ...current, [key]: value })); setErrors((current) => ({ ...current, [key]: undefined })); }} onSubmit={save} />}</section>;
}

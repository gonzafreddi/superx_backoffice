export const toCents = (value) => { const match = String(value ?? "").trim().match(/^(-?)(\d+)(?:[.,](\d{1,2}))?$/); if (!match) throw new Error("Monto inválido"); return BigInt(`${match[1]}${match[2]}`) * 100n + BigInt(`${match[1]}${(match[3] ?? "").padEnd(2, "0")}`); };
export const centsToDecimal = (value) => { const sign = value < 0n ? "-" : ""; const absolute = value < 0n ? -value : value; return `${sign}${absolute / 100n}.${String(absolute % 100n).padStart(2, "0")}`; };
export const suggestDueDate = (issueDate, days) => { if (!issueDate) return ""; const date = new Date(`${issueDate}T12:00:00`); date.setDate(date.getDate() + Math.max(0, Number(days) || 0)); return date.toISOString().slice(0, 10); };
export const validateInvoice = (input) => { const errors = {}; if (!/^\d{1,5}$/.test(String(input.pointOfSale ?? ""))) errors.pointOfSale = "Ingresá entre 1 y 5 dígitos."; if (!/^\d{1,8}$/.test(String(input.number ?? ""))) errors.number = "Ingresá entre 1 y 8 dígitos."; if (!input.issueDate) errors.issueDate = "Indicá la emisión."; if (!input.dueDate || input.dueDate < input.issueDate) errors.dueDate = "El vencimiento no puede ser anterior a la emisión."; for (const key of ["subtotal", "discountTotal", "taxTotal"]) try { toCents(input[key]); } catch { errors[key] = "Ingresá un importe decimal válido."; } return errors; };
export const invoiceStatus = (status, paymentStatus) => status === "VOIDED" ? { label: "Anulada", tone: "order-cancelled" } : ({ UNPAID: { label: "Sin pagar", tone: "inactive" }, PARTIALLY_PAID: { label: "Pago parcial", tone: "active" }, PAID: { label: "Pagada", tone: "active" }, OVERDUE: { label: "Vencida", tone: "order-cancelled" } }[paymentStatus] ?? { label: "—", tone: "inactive" });
